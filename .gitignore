# Ignore the _site folder (generated by Jekyll)
_site/

# Ignore Gemfile.lock file (you typically don't need to track this for Jekyll sites)
Gemfile.lock

# Ignore any system files or IDE-specific files
.DS_Store           # macOS Finder metadata
.idea/              # JetBrains IDEs
.vscode/            # Visual Studio Code settings
*.swp               # Swap files (Vim)

# Ignore temporary files
*.log               # Log files
*.tmp               # Temporary files
*.bak               # Backup files

# Ignore environment-specific files
.env                # Environment variables (useful for local secrets)
*.env               # Additional .env files for different environments

# Ignore bundled gems in the system (this is specific to the Ruby environment)
.bundle/

# Ignore package manager directories for front-end tools
node_modules/       # Node.js dependencies if using Node.js for build processes
npm-debug.log       # npm debug logs

# Ignore other files that could be generated or are unnecessary for source control
config.yml~         # Backup copy of config
.jekyll-metadata    # Metadata used by Jekyll

# Ignore generated cache files (Ruby gem cache)
vendor/             # Vendored gems (if you're using bundler)
